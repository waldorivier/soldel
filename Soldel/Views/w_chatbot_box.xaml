<Window x:Class="mupeModel.Views.chatbot_box"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:commands="clr-namespace:mupeModel.Commands"
        mc:Ignorable="d"
        Title="chatbot_box" Height="300" Width="300">
    <Window.CommandBindings>
        <CommandBinding Command="commands:chat_box_command.validate"
                        CanExecute="validate_can_execute"
                        Executed="validate_executed"/>
    </Window.CommandBindings>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="50"/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0" Margin="2">
            <StackPanel>
                <TextBlock  x:Name="question"/>
            </StackPanel>
        </Grid>
        <Grid Grid.Row="1" Margin="2">
            <TextBox x:Name="response"/>
        </Grid>
        <Grid Grid.Row="2" Margin="2">
            <StackPanel >
                <Button x:Name="btn_validate" Content="Valdider" Command="commands:chat_box_command.validate"/>
                <Button x:Name="btn_cancel" Content="Annuler"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
