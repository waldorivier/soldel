<Window x:Class="mupeModel.Views.chatbot_box"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:mupeModel.Views"
        mc:Ignorable="d"
        Title="chatbot_box" Height="300" Width="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="50"/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0" Margin="2">
            <StackPanel>
                <TextBlock>
                </TextBlock>
            </StackPanel>
        </Grid>
        <Grid Grid.Row="1" Margin="2">
            <TextBox>
            </TextBox>
        </Grid>
        <Grid Grid.Row="2" Margin="2">
            <StackPanel >
                <Button x:Name="btn_tree_add" Content="Continuer"/>
                <Button x:Name="btn_tree_delete" Content="Annuler"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
