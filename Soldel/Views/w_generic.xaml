<Window x:Class="Soldel.Views.w_generic"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:mupeModel"
        mc:Ignorable="d"
        Title="GENERIC" Height="600" Width="600">

    <Grid x:Name="object_list">
        <Grid.RowDefinitions>
            <RowDefinition Height="30"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row = "0">
            <StackPanel>
                <ComboBox x:Name="cb_connection" Height="25" Padding="5"/>
            </StackPanel>
        </Grid>
       
        <Grid Grid.Row = "1">
            <Grid.ColumnDefinitions >
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="5"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TreeView x:Name="tree_main" Grid.Column="0">
                <TreeView.Resources>
                    <HierarchicalDataTemplate DataType="{x:Type local:pe_grmu}" ItemsSource="{Binding pe_gmmu_list}">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{Binding no_ip}" />
                        </StackPanel>
                    </HierarchicalDataTemplate>
                    <HierarchicalDataTemplate DataType="{x:Type local:pe_gmmu}" ItemsSource="{Binding pe_muta.pe_attr_list}">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{Binding pe_muta.libf_muta}"/>
                        </StackPanel>
                    </HierarchicalDataTemplate>
                    <DataTemplate DataType="{x:Type local:pe_attr}">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{Binding nom_attr}"/>
                        </StackPanel>
                    </DataTemplate>
                </TreeView.Resources>
            </TreeView>
            <GridSplitter VerticalContentAlignment="Stretch" Grid.Column="1"/>
            <Grid Grid.Column="2">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="5"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <DataGrid x:Name="dg_detail" Grid.Row="0"/>
                <GridSplitter HorizontalAlignment="Stretch" Grid.Row="1"/>
                <DataGrid x:Name="dg_list" Grid.Row="2" ItemsSource="{Binding ElementName=tree_main, Path=SelectedItem.Content}">
                    <!--<DataTemplate DataType="{x:Type local:pe_attr}">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{Binding nom_attr}"/>
                        </StackPanel>
                    </DataTemplate> />-->
                </DataGrid>
            </Grid>
        </Grid>
    </Grid>
</Window>
